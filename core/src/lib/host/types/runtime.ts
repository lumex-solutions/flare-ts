/**
 * The runtime identity vocabulary: which environment a host is running in.
 */

/**
 * Supported runtime environments for a {@link FlareHost}.
 */
export type HostRuntime = "node" | "bun" | "deno" | "cloudflare";
